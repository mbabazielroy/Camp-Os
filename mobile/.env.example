# Where the web app is running.
# Local testing: your computer's LAN IP + port 3000 (run `npm run dev:lan` at the repo root)
# Production: your deployed URL
EXPO_PUBLIC_APP_URL=http://192.168.1.42:3000
